let base = prompt("Ingrese una base");
let altura = prompt("Ingrese una altura");

const calcularArea = (parametro1, parametro2) => {
    area = parametro1*parametro2;
    alert(area);
}

calcularArea(base, altura);
